import React from 'react';
import {
  StyleSheet,
  Text,
  View,
} from 'react-native';
import LottieFilesPicker from './LottieFilesPicker';

const styles = StyleSheet.create({
});

export default class App extends React.Component {
  render() {
    return (
      <LottieFilesPicker
      />
    );
  }
}
